import { reader } from './reader'

// Typed page content helpers.
// These functions will be populated in Phase 3 when singletons and collections are defined.
export async function getHomePageContent() {
  // Phase 3: return reader.singletons.homePage.read()
  return null
}
