import { reader } from './reader'

// Typed collection helpers.
// These functions will be populated in Phase 3 when collections are defined.
export async function getBoardMembers() {
  // Phase 3: return reader.collections.boardMembers.all()
  return []
}
